

const nextConfig = {
  images: {
    // Add remotePatterns here if you ever use external image URLs again.
    // All images are now served from /public/images/ locally.
  },
};

export default nextConfig;
